# Womo Config

Este folder queda reservado para configuración separada del proyecto.

Por seguridad en esta fase no se movió todavía la configuración Firebase fuera del core para no arriesgar el build estable. El siguiente paso recomendado es extraer `firebaseConfig` a un archivo controlado aquí cuando validemos que esta separación funciona igual que la versión estable.
